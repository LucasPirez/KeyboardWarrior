{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "EmailConfiguration": {
    "Port": "${{ EMAIL_PORT }}",
    "Host": "${{ EMAIL_HOST }}",
    "Password": "${{ EMAIL_PASSWORD }}",
    "Email": "${{ EMAIL_ADDRESS }}"
  },
  "MyEmail": "${{ MYEMAIL_ADDRESS }}"
}
